# Standards: Lambda POST Endpoint MVP

## Applicable Standards

No standards currently apply to this spec. The `agent-os/standards/index.yml` is empty — this is a fresh project with no established conventions yet.

## Notes

As the project grows, standards may be established for:
- Python code style and structure
- Terraform module conventions
- CI/CD pipeline patterns
- Naming conventions for AWS resources

When standards are added to `agent-os/standards/index.yml`, future specs should reference and follow them.
